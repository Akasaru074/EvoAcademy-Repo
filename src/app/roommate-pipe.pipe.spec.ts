import { RoommatePipePipe } from './roommate-pipe.pipe';

describe('RoommatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RoommatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
